# gateway-h3 - the same shape as compose.gateway.yml, reached over QUIC
# instead, and the one profile of the three that still runs Caddy: stock nginx
# has no QUIC listener, and Caddy is at the top of the board here rather than
# behind it. It binds 8443 on tcp and udp with `protocols h1 h2 h3`, so the
# config in proxy-h3/ needs nothing h3-specific and only the load generator
# differs. The proxy to server hop stays loopback h1 either way.
services:
  proxy:
    build:
      context: ./proxy-h3
    network_mode: host
    cpuset: "${PROXY_CPUSET:-0-15,64-79}"
    ulimits:
      memlock: -1
      nofile:
        soft: 1048576
        hard: 1048576
    security_opt:
      - seccomp:unconfined
    volumes:
      - ${CERTS_DIR}:/certs:ro
      - ${DATA_DIR}/static:/data/static:ro
    depends_on:
      - server

  server:
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: host
    cpuset: "${SERVER_CPUSET:-16-31,80-95}"
    ulimits:
      memlock: -1
      nofile:
        soft: 1048576
        hard: 1048576
    security_opt:
      - seccomp:unconfined
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - DATABASE_MAX_CONN=256
      - DATASET_PATH=/data/dataset.json
    volumes:
      - ${DATA_DIR}/dataset.json:/data/dataset.json:ro
      - ${DATA_DIR}/static:/data/static:ro
